library(turmite59)

turmite59(
  shade = "#00c7a6",
  image_dpi = 600,
  heart_grain = 5000,
  unfold_octaves = 16,
  turmite_stepsize = 2)

turmite59(
  shade = "#F74714",
  image_dpi = 600,
  heart_grain = 5000,
  unfold_octaves = 16,
  turmite_stepsize = 2)